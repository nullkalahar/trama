função principal()
    metricas_reset()
    camp = campanha_criar("MetricaCamp", {"titulo": "M"}, {"grupo": "A"})
    campanha_executar(camp["id"], ["u1", "u2", "u3", "u4"])

    m = metricas_snapshot()
    exibir(m != nulo)
    exibir(tamanho(m["counters"]) >= 1)
fim
