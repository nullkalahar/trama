função principal()
    app = web_criar_app()
    web_adicionar_rota_json(app, "GET", "/api/v1/usuarios", {"ok": verdadeiro, "dados": []}, 200)
    out = web_gerar_sdk(app, ".local/cliente_api_v206.py", "python", "ClienteUsuarios")
    exibir(out["ok"])
    exibir(out["linguagem"])
fim
