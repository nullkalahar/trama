função principal()
    app = web_criar_app()
    web_adicionar_rota_json(app, "GET", "/saude", {"ok": verdadeiro}, 200)
    resultado = web_exportar_openapi(app, ".local/openapi_v206.json", "API Exportada", "2.0.6", "http://localhost:8080")
    exibir(resultado["ok"])
    exibir(resultado["arquivo"])
fim
