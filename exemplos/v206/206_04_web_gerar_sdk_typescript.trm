função principal()
    app = web_criar_app()
    web_adicionar_rota_json(app, "POST", "/api/v1/eventos", {"ok": verdadeiro}, 201)
    out = web_gerar_sdk(app, ".local/cliente_api_v206.ts", "typescript", "ClienteEventos")
    exibir(out["ok"])
    exibir(out["linguagem"])
fim
