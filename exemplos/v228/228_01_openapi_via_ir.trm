função ping(req)
    retorne {"status": 200, "json": {"ok": verdadeiro, "dados": {"pong": verdadeiro}, "meta": {"v": "v1"}}}
fim

assíncrona função principal()
    app = web_criar_app()
    web_rota(app, "GET", "/api/v1/ping/:id", ping)
    spec = web_gerar_openapi(app, "API IR", "2.1.28", "http://127.0.0.1:8080")
    exibir(spec["openapi"])
fim
