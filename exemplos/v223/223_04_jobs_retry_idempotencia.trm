função tarefa(payload)
    retorne payload["id"]
fim

assíncrona função principal()
    fila = fila_criar("idempotencia")
    a = aguarde fila_enfileirar(fila, tarefa, {"id": 1}, 2, 3.0, "chave-1")
    b = aguarde fila_enfileirar(fila, tarefa, {"id": 1}, 2, 3.0, "chave-1")
    r = aguarde fila_processar(fila)

    exibir(a["enfileirado"])
    exibir(b["idempotente"])
    exibir(r["concluidos"])
fim
