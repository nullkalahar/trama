assíncrona função principal()
    payload = {"evento": "pedido.criado", "id": "p1"}
    r = aguarde webhook_enviar(
        "http://127.0.0.1:9999/webhook",
        payload,
        "segredo_local",
        1,
        1.0
    )
    exibir(r["ok"])
fim
