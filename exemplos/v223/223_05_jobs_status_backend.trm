função tarefa(payload)
    retorne payload
fim

assíncrona função principal()
    fila = fila_criar_com_backend("status", "memoria")
    aguarde fila_enfileirar(fila, tarefa, {"ok": verdadeiro})
    s1 = fila_status(fila)
    aguarde fila_processar(fila)
    s2 = fila_status(fila)

    exibir(s1["backend"])
    exibir(s2["concluidos"])
fim
