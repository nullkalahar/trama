assíncrona função suite_74()
    t = criar_tarefa(esperar(0.001))
    aguarde com_timeout(t, 1.0)
    retorne {
        "suite": "v213_74",
        "ok": verdadeiro,
        "categoria": "operacao_release"
    }
fim

assíncrona função principal()
    exibir(aguarde suite_74())
fim
